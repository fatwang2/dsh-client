{
  "name": "dsh-client-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Manifest for the closed production dependency tree of the dsh CLI staged into the packaged app. Pinned exactly: the harness is a fast-moving release candidate with no compatibility promise.",
  "dependencies": {
    "@deepseek-ai/dsh": "0.1.0-rc.6"
  }
}
